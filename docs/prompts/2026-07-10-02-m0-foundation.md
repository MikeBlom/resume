# Session 2026-07-10 #2 — M0 build and real content integration

- **Tooling:** Claude Code (model: Fable 5)
- **Human:** Mike Blom
- **Output:** M0 shipped — public repo, docs skeleton, content model, `/resume` page, CI;
  then real resume content integrated from Mike's questionnaire answers and resume document

## What was asked

Continue from the approved development plan: initialize the public repository with the
transparency skeleton, build the typed content model and printable resume page, set up CI, and
publish. Then, after Mike answered `docs/requirements/questionnaire.md` and provided his resume
document: replace all placeholder content with real career data.

## What the AI did (directed)

1. Scaffolded Vite + React + TypeScript with lint/typecheck/test/format scripts and CI.
2. Wrote the transparency skeleton: requirements, ADRs 0001–0003, redaction checklist,
   workflow, cost ledger + methodology, review/testing/design/retrospective seeds.
3. Built the zod content model with placeholder flags and schema/integrity tests.
4. Built `/`, `/resume` (semantic, printable), and `/how-it-was-built`; created the public
   GitHub repository and verified CI green.
5. Extracted real content from Mike's resume document into the content model: Instructure,
   MasteryConnect, high-level military/government service, skills, languages, leadership
   philosophy; education omitted by Mike's choice.

## Human decisions of record

- AI attribution stance confirmed: repo docs document the AI process; commits are authored by
  Mike alone.
- Contact is LinkedIn-only; education unreferenced; paper route is story color, not resume.
- Military content stays super high level (10+ years, combat zones worldwide, no units/dates
  beyond the public resume's range).
- Art direction: 16-bit, playful, text-only, family included generally, no logos, photos as
  reference only.

## Redaction actions this session

- Resume PDF **held out of the repo**: it contains phone, personal email, and home city while
  the public contact stance is LinkedIn-only. Publication decision pending a sanitized version.
- Questionnaire answers curated before commit: family specifics generalized (no children's
  ages), local file paths removed.

## Follow-up (same day)

Mike chose to resolve the PDF question by **generating a sanitized PDF from the site itself**:
`scripts/generate-resume-pdf.mjs` renders the `/resume` print view with Playwright to
`public/Mike-Blom-Resume.pdf` (`npm run pdf`). The PDF therefore always matches the single
content source and contains no private contact details by construction — verified by reading
the generated output before commit. Also this session: the Cloudflare Workers Builds
integration failed on its first runs; `wrangler.jsonc` (assets-only Worker, SPA fallback) was
added, with the remaining dashboard build-settings fix in Mike's hands.
